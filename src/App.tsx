import "./App.css";

interface appProps {
  settings: {
    iso: string;
    isoFolder: string;
    slippiDirectory: string;
  };
}

function App(props: appProps) {
  return <main></main>;
}

export default App;
